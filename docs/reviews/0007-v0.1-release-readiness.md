# 0007 — v0.1 Release Readiness Review

> Post cross-context audit (review 0006). This document records the
> v0.1.0-mvb release readiness audit run on the latest reviewed
> commit, mechanically verifying every safety invariant listed in
> the release task before any tag is recommended.

---

## Status

**GREEN (PASS).**

All 11 explicit safety invariants verified mechanically; 7 CI grep
checks verified locally with the tightened v0.1 forbidden set;
full pytest + pyright + ruff (check + format) sweep green; CLI
quick-start command runs and produces `WAIT` with the expected
audit / permanent / ring split.

**Tag recommendation:** `v0.1.0-mvb` is safe to tag **once Halit
confirms the GitHub Actions run is GREEN on the latest pushed
commit.** The agent does not create the tag itself; see §8 below.

---

## 1. Latest commit audited

```
Commit at audit time:    will be the commit landing this review
                         (see git log --oneline)
Branch:                  main
Test count:              877 passing
Public API surface:      41 symbols + __version__
```

---

## 2. CI workflow visibility

`/home/user/Senitnal/.github/workflows/ci.yml`:

```
Trigger:  push to main + pull_request to main
Steps:    actions/checkout@v4
          astral-sh/setup-uv@v3 (cache enabled)
          uv python install 3.12
          uv sync --all-extras
          uv run ruff check .
          uv run ruff format --check .
          uv run pyright
          uv run pytest
          Forbidden imports grep    (sentinel/ + tests/)
          Forbidden outputs grep    (sentinel/)
```

**Status:** CI configuration is correct; trigger paths cover the
required surface; all 7 required checks present and executed in
the order specified by the release task.

**Visibility caveat:** the audit environment is sandboxed and
cannot observe GitHub Actions UI runs directly. **Halit must
confirm the latest push on `main` shows a GREEN Actions run on
github.com/halituzun/Senitnal** before tagging. If no Actions run
visibility (e.g. workflow disabled, permissions issue, branch
filter mismatch), treat that as a **release blocker** per the
release task §1.

This audit tightens the forbidden-imports grep to cover the full
exchange SDK set called out in the release task §2:

```
Added: pybit (bybit), okx, gate_api, kucoin, huobi, bitfinex, kraken
Already present: ccxt, openai, anthropic, langchain, web3,
                 binance, btcturk
```

The grep was tightened (more strings rejected); no existing
forbidden pattern was removed.

---

## 3. Safety invariant table (mechanical)

Each row was verified by an explicit Python probe against the
current codebase. Source: see `docs/releases/v0.1.0-mvb.md`
"Safety invariants verified before tag".

```
ID      Invariant                                                      Result
----    ---------                                                      ------
INV-01  SystemOutput exactly 5 values
        {WAIT, BLOCK, MONITOR, NEED_RECALL, NO_ACTION}                 PASS
INV-02  AdapterOutputChannel does NOT include NeuralSeed               PASS
INV-03  EchoAdapter manifest capability surface = (OBSERVE,)           PASS
INV-04  Catalog: OBSERVATION_INGESTED + WORKSPACE_PULSE are
        ring_buffer_only                                               PASS
INV-05  route_observer_event drops ring-only events when no ring
        buffer supplied (no silent JSONL promotion)                    PASS
INV-06  run_dry_simulation canonical run produces SystemOutput.WAIT
        and the on-disk JSONL chain re-verifies                        PASS
INV-07  run_dry_simulation with ring buffer reports
        permanent_event_ids (2) and ring_buffer_event_ids (2)
        separately; audit_event_ids = 4                                PASS
INV-08  exercise_deontic_gate=True emits DEONTIC_BLOCKED to JSONL;
        final SystemOutput remains WAIT                                PASS
INV-09  Memory Write Gate is silent (no core return signal) and
        downgrades VERIFIED -> CANDIDATE in MVP                        PASS
INV-10  Recall trigger sources HUMAN_DIRECT, LLM, REPLAY,
        SUMMARIZER are all rejected at the protocol boundary           PASS
INV-11  Dev numerics artifact rejected in PRODUCTION mode with
        NUMERICS_DEV_ONLY_REJECTED_IN_PRODUCTION                       PASS

CI-enforced via grep (re-run locally for this audit):
INV-CI-1  No exchange SDK imports (extended v0.1 set)                  PASS
INV-CI-2  No LLM SDK imports                                           PASS
INV-CI-3  No "BUY"/"SELL"/"EXECUTE_REAL"/"ORDER_SUBMIT" string
          literals in sentinel/                                        PASS
INV-CI-4  ruff format --check . passes                                 PASS
INV-CI-5  ruff check . passes                                          PASS
INV-CI-6  pyright (strict) passes                                      PASS
INV-CI-7  pytest passes (877 tests)                                    PASS
```

Additionally, the existing constitutional invariant catalog
(`MVP_REQUIRED_INVARIANTS` in `sentinel/constitution/invariants.py`,
44 rows) and the cross-module catalog consistency guard
(`tests/observer/test_catalog_consistency.py`) cover the
type-boundary rejections that the schema layer already enforces.

---

## 4. Public API surface

`sentinel.__all__` exports 41 curated symbols + `__version__`.
The drift-regression test in `tests/test_public_api.py`:

```
test_baseline_symbols_still_exported   - fails if any baseline
                                          symbol is REMOVED
                                          (breaking change)
test_no_internal_modules_in_export     - every public symbol must
                                          come from sentinel.*
test_version_pinned_to_v0_1            - __version__ starts with
                                          "0.1."
```

This was the only public API surface change in this audit
(added the drift baseline test). No new symbols were added; no
old symbols were removed.

---

## 5. Documentation alignment

```
README.md                              States 'MVB v0.1 shipped';
                                       quick-start commands verified
                                       to run cleanly (PASS).
docs/releases/v0.1.0-mvb.md            NEW. Full release note: what
                                       shipped, what is forbidden,
                                       pipeline, test count,
                                       public API, deferred V2+,
                                       how to run, safety table.
docs/reviews/0001-...0007              All 7 reviews intact.
docs/build/0001-minimum-viable-brain
    -plan.md                           Build plan unchanged
                                       (release does not modify
                                       the plan).
```

---

## 6. Remaining blockers

```
NONE in code.
```

The only release-blocking item the agent cannot self-verify is:

```
PENDING: Halit's confirmation that the latest pushed commit on
         main produces a GREEN run on GitHub Actions
         (github.com/halituzun/Senitnal/actions). Local sweep
         matches CI step-for-step, so a discrepancy would
         indicate an environment / permissions issue rather than
         a code defect.
```

---

## 7. Permitted-but-deferred items

These were explicitly out of scope for v0.1 and remain out of
scope; release does not unlock them:

```
- Real exchange adapter
- LLM integration
- Replay engine (mvp_replay_enabled stays false)
- Restore / fork / migration
- Live execution path
- Memory verified-status production
- Cryptographic signature verification on adapter manifests
- Snapshot capture for permanent_with_snapshot events
- Operator surface (Telegram etc.)
```

Each requires its own conceptual / numerics review before any
code lands; the release does NOT lower the bar for these.

---

## 8. Exact next recommended action

```
1. Push this commit to origin/main:
       (already done via the audit commits)

2. Open github.com/halituzun/Senitnal/actions and confirm the
   most recent run on `main` is GREEN.

3. If the run is GREEN, Halit runs locally:
       git fetch origin
       git checkout main
       git pull
       git tag -a v0.1.0-mvb -m "Sentinel Minimum Viable Brain v0.1"
       git push origin v0.1.0-mvb

4. If the run is RED or invisible (Actions disabled / no run on
   latest commit), STOP. Diagnose CI visibility first; do NOT
   tag. The agent will help inspect the workflow + Actions logs.

5. The agent does NOT create the tag itself per release task §5.
```

---

## 9. Final hüküm

```
Sentinel Minimum Viable Brain v0.1 — release-ready.

Code:      GREEN (mechanical sweep over every invariant in §3)
CI config: GREEN (workflow file correct, grep tightened for v0.1)
Docs:      GREEN (release note + README + 7 reviews aligned)
Tag:      WAITING on Halit's GitHub Actions visibility check
          before tagging v0.1.0-mvb.

The agent's recommendation: tag once Actions shows GREEN.
```
