# Panel Sayfa Düzeltme Planı

## Tespit Edilen Sorunlar

### 1. NavBar: Çift "Trust" etiketi (Kritik)
**Dosya:** `panel/web/src/components/NavBar.vue:25-26`
- Satır 25: `/adapter-trust` → "Trust"
- Satır 26: `/source-trust` → "Trust" (aynı etiket!)
- **Düzeltme:** `/adapter-trust` → "Trust", `/source-trust` → "Sources"

### 2. ExchangesPage: Eksik alanlar (Kritik)
**Dosya:** `panel/web/src/pages/ExchangesPage.vue:22-26`
- Sayfa `ex.region`, `ex.connection_mode`, `ex.last_checked_ms`, `ex.pairs_tracked` bekliyor
- Snapshot'ta sadece `exchange_id, name, status, trade_enabled, withdraw_enabled, latency_ms` var
- **Düzeltme:** ExchangesPage template'ini snapshot'ta var olan alanlarla uyumlu güncelle
  - `ex.region` → kaldır veya `ex.exchange_id`'den türet
  - `ex.connection_mode` → `"read-only"` sabit değer
  - `ex.last_checked_ms` → `Date.now()` (snapshot'ta yok, anlık)
  - `ex.pairs_tracked` → kaldır veya sabit değer

### 3. Learning Loop: Eksik event tipleri (Kritik)
**Dosya:** `sentinel/runtime/learning_loop.py`
- IngressPreview sayfası `INGRESS_COMPILED` event'leri bekliyor → loop'ta YOK
- Replay sayfası `REPLAY_TRIGGERED/REPLAY_COMPLETED` bekliyor → loop'ta YOK  
- AuditTrail sayfası event_type içinde "AUDIT" geçen event'leri filtreliyor → loop'ta YOK
- **Düzeltme:** Learning loop'a her N cycle'da bir bu event'leri ekle:
  - Her 3 cycle'da bir `INGRESS_COMPILED` event
  - Her 5 cycle'da bir `REPLAY_COMPLETED` event
  - Her 10 cycle'da bir `CONSTITUTION_AUDIT` event (event_type "AUDIT" içeriyor)

### 4. SourcesPage: Label çakışması (Orta)
**NavBar'daki "Sources" linki:** Intelligence altında `/sources` sayfası "Intelligence Sources" başlıklı. Adapters altında `Trust` linki `/source-trust` sayfasına gidiyor. `/source-trust` sayfası "Source Trust" başlıklı. NavBar'da düzeltildikten sonra (`/source-trust` → "Sources" etiketi) daha anlaşılır olacak.

### 5. Snapshot: Adapter health çeşitliliği eksik (Düşük)
**Dosya:** `sentinel/runtime/learning_loop.py` export_snapshot fonksiyonu
- Tüm adapter'lar %97+ healthy çıkıyor (random > error_rate * 2)
- Hiç quarantined/stale adapter yok
- **Düzeltme:** 1 adapter'ı stale, 1 adapter'ı quarantined yap (cycle mod ile dönüşümlü)

### 6. Snapshot: PnL dağılımı dengesiz (Düşük)
- Sadece 2 aktif strateji PnL üretiyor (BTC ve ETH)
- SOL stratejisi LIMITED_LIVE olmasına rağmen hiç PnL almıyor
- **Düzeltme:** SOL stratejisini de PnL havuzuna ekle

---

## Uygulama Planı

### Adım 1: NavBar etiket düzeltmesi
`panel/web/src/components/NavBar.vue` satır 26: `Trust` → `Sources`

### Adım 2: ExchangesPage template düzeltmesi
`panel/web/src/pages/ExchangesPage.vue`:
- `ex.region` referansını kaldır / sabit değer koy
- `ex.connection_mode` → `"read-only"` 
- `ex.last_checked_ms` yerine `ex.latency_ms` veya sabit değer
- `ex.pairs_tracked` sütununu kaldır

### Adım 3: Learning loop'a eksik event'leri ekle
`sentinel/runtime/learning_loop.py` `run_cycle` fonksiyonuna:
- Her 3 cycle'da: `_event(state, "INGRESS_COMPILED", "INFO", "ingress-compiler", None, ...)`
- Her 5 cycle'da: `_event(state, "REPLAY_COMPLETED", "INFO", "replay-engine", strategy.strategy_id, ...)`
- Her 10 cycle'da: `_event(state, "CONSTITUTION_AUDIT", "INFO", "constitution-auditor", None, ...)`

### Adım 4: Adapter health çeşitliliği
`export_snapshot` içinde `ADAPTER_POOL`'dan bir adapter'ı stale, birini quarantined yap.

### Adım 5: SOL stratejisine PnL dağıt
`run_cycle` içinde SOL stratejisi için PnL hesaplamasını da aktif et.

---

## Doğrulama

1. `pnpm lint` panel/web ve panel/api → 0 hata
2. `pnpm test` panel/api (33 test) + panel/web (3 test) → PASS
3. `uv run pytest` → 1789 PASS
4. `uv run python sentinel/runtime/learning_loop.py --cycles 5` → tüm event tipleri oluşuyor
5. Panel'de tüm sayfaları tek tek gez: Dashboard, Evidence, Ledger, Ingress, Sources, Decisions, Memory, Replay, Portfolio, Health, Policy, Audit Trail, Adapters, Trust(×2), Exchanges, Credentials, Ideas → hepsi veri gösteriyor, hata yok
