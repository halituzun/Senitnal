"""M0 — Implicit neural tissue (minimal MVP stub).

Per build plan §10 (Phase 5):
    - neuron.py:         neuron + receptor profile (D §4, S §8)
    - synapse.py:        synapse weight + charge propagation (learning OFF in MVP)
    - tissue.py:         seed neuron uniform allocation (S §6 divergence = 0)
    - assembly.py:       candidate detection (stable_assembly_count_at_birth = 0)
    - self_field.py:     homeostatic > predictive > narrative (S §11)
    - proto_resonance.py: 5-layer invariant (S §9)

MVP: learning OFF; stable assembly empty triplet enforced at birth.
"""
