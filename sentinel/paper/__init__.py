"""V7 — Paper co-pilot package.

Non-executing advisor surface.  ``PaperDecision`` outputs are drawn
from the closed v0.1 ``SystemOutput`` set; no path constructs an
action-intent object or writes to Gel.Al.

See ``docs/build/0008-v7-paper-co-pilot-build-plan.md`` for the
full contract.
"""
